import { UserInfo, Answer, AssessmentResult } from "@/types/aiQuotient"; export const prepareHubspotData = (userInfo: UserInfo, result: AssessmentResult, answers: Answer[]): Record<string, any> => { const hubspotData: Record<string, any> = { firstname: userInfo.firstName, lastname: userInfo.lastName, email: userInfo.email, company: userInfo.company, company_size: userInfo.companySize, ai_quotient_score: result.percentage, ai_readiness_level: result.readinessLevel }; return hubspotData; };
